[package]
name = "wtg-core"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true

[dependencies]
# NVML bindings
nvml-wrapper = "0.9"

# Logging
tracing = "0.1"

