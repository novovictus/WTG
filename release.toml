# Minimal cargo-release config for WTG
allow-branch = ["main"]

publish = false
push = true
push-remote = "origin"

tag = true
tag-name = "v{{version}}"
tag-message = "Release v{{version}}"

shared-version = true
consolidate-commits = true